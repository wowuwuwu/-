@import "./styles/iconfont.wxss";
/* 不支持*通配符 所以我们只能单个设 */
page,view,text,image,button,swiper,swiper-item,navigator,checkbox,rich-text,radio{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}
image{
    width:100%;
}
page{
    /* 设置主题色 后面直接调用 */
    --themeColor:#eb4450;
}